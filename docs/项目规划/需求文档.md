# 设备 ↔ 电脑端 双通道低延迟语音传声机 · 需求文档 V3.0

> 主控：ESP32-S31 双核 RISC-V @320MHz
> 存储：16MB Flash + 16MB Octal PSRAM
> 音频：ES8389 Codec / 双 MEMS 麦 / 双 NS4150B 功放
> 屏幕：4.3" RGB 800×480 ST7262E43 + 电容触摸
> ESP-IDF：v6.1.0（实际工程版本，文档早期写 v6.2.0，以工程为准）
> PC 端：Python 3.10+，socket + pyaudio + numpy

---

## 1. 项目概述

本系统由 **ESP32-S31 设备端** 与 **PC 端软件** 两部分组成，通过 Wi-Fi 局域网连接，提供两种互斥工作模式：

- **UDP 实时通话模式**——为"听到对方"优化，20ms 一帧、乱序重排、丢包填静音，端到端 ≤100ms，允许偶发卡顿但绝不等待；
- **TCP 云端录音模式**——为"事后回听完整录音"优化，可靠流式传输、整段落盘 WAV，零丢帧，可断点续传与掉电保护。

两种模式共用同一套 I2S/Codec 驱动与帧格式，仅传输层与播放策略不同，由 UI 触控或物理按键一键切换。

---

## 2. 系统总体架构（双通道）

```
ESP32-S31 设备端  ⇄  Wi-Fi 局域网  ⇄  PC 端软件（Python）

【通道 A · UDP 实时通话】端口 50000/50001
  麦克风 I2S DMA → 20ms PCM 帧(640B) → UDP 发 → PC pyaudio 实时扬声器播放
  PC 麦克风 → UDP 发 → 设备 I2S TX DMA 播放（乱序重排 / 超时填静音）

【通道 B · TCP 整段录音】端口 50002
  设备麦克风 → 连续 PCM 流 → TCP Client → PC TCP Server → 写入 WAV 文件
  （可选上行）PC 音频 → TCP → 设备播放整段音频

信令通道 UDP 50003：模式切换、开始/停止录音、音量、设备发现（UDP 广播）
```

---

## 3. 硬件平台（Korvo-1 V1.1）

| 模块 | 规格 | 连接 | 用途 |
|---|---|---|---|
| 主控 | ESP32-S31 双核 RISC-V @320MHz，512KB SRAM | — | Core1 跑音频硬实时；Core0 跑 Wi-Fi/网络/UI |
| 存储 | 16MB Flash + 16MB Octal PSRAM | Octal | PSRAM 放 LVGL 帧缓冲与环形缓冲；Flash 存 UI 资源/字库/NVS |
| 音频 Codec | ES8389 立体声（I2C 0x20） | I2C SDA=GPIO0/SCL=GPIO1；I2S SCLK=GPIO3/LRCK=GPIO4；**MCLK=GPIO42 未连接** | `use_mclk=false`，BCLK 恢复时钟，TDM 帧格式 |
| 音频 I/O | 双 MEMS 麦 + 双 NS4150B 功放 | ES8389 ADC/DAC | 16kHz/16bit；PA GPIO 使能控制静音防爆音 |
| 显示触控 | 4.3" RGB 800×480 ST7262E43 + 电容触摸 | 16-bit RGB 并行 | LVGL：模式选择、通话计时、录音时长、音量、电平表 |
| 按键 | BOOT / RST | GPIO | BOOT 短按=PTT/通话，长按 3s=切换 UDP/TCP 模式或配网 |

> **硬件坑位**：① ES8389 必须 `use_mclk=false`；② 收发走 TDM，不能配普通 I2S；③ 官方推荐 16kHz，正好对齐 20ms 语音帧（640B）。

---

## 4. 模式一：UDP 低延迟实时通话

### 4.1 设备端行为
- I2S RX DMA 双缓冲，每 20ms 产生一帧 640B PCM（16kHz/16bit/单声道）；
- 帧头：`magic(0xA5) + seq(2B) + timestamp(4B) + length(2B) + flags(1B)`；
- UDP 发往 PC IP:50000；同时在 50001 收 PC 回传语音；
- 接收侧 16 帧环形重排窗口，乱序帧重排；超时 40ms 未到则填静音补偿；
- 本地采集→播放回环延迟 **≤8ms**；端到端（经 Wi-Fi）**≤100ms**；
- 支持全双工常开 / PTT 半双工（长按 200ms 防误触）两种，UI 可切；
- 可选 G.722 编码（64kbps）压缩带宽，裸 PCM 为默认模式便于联调。

### 4.2 PC 端行为
- Python 客户端：socket UDP 收包 + pyaudio 输出到扬声器；
- 本地麦克风经 pyaudio 采集，按 20ms 组帧，加相同帧头发往设备 50001；
- 抖动缓冲（jitter buffer）：默认缓存 2~3 帧（40~60ms）平滑网络抖动；
- 丢包统计：每 2s 打印丢包率/平均延迟/抖动；UI 同步显示；
- CLI 参数：`--mode udp --host <设备IP> --port 50000 --ptt`；
- Windows/macOS/Linux 跨平台；pyaudio 设备可选输入/输出设备。

---

## 5. 模式二：TCP 整段录音记录

### 5.1 设备端行为
- 作为 TCP Client 连接 PC:50002，链路建立后持续上行 PCM 流；
- 流格式：先 44B WAV 头（占位），随后原始 PCM；结束时发 EOF 包并由 PC patch WAV 头；
- TCP 滑动窗口 + 阻塞发送，保证 **零丢帧**；发送缓冲区水位低时通知 UI 提示网络拥塞；
- 支持断点续传：断线重连后在包头附 offset，PC 从偏移处续写；
- 掉电保护：每 2s 一次 flush 到 PC 并更新文件索引；
- 按时长切片：每 30 分钟自动切一个新 WAV，文件名 `rec_YYYYMMDD_HHMMSS.wav`；
- 录音期间设备本地喇叭静音（仅上行），避免自采自录回声。

### 5.2 PC 端行为
- TCP Server 监听 50002，接受设备连接后开始落盘；
- 按设备发的 WAV 头创建文件，持续写入；EOF 到达后回填采样率/长度数据；
- 存储目录可配置（默认 `./recordings/`），自动建索引 `index.csv`（时间/时长/大小）；
- 支持边录边播：实时波形、总时长、磁盘剩余空间显示；
- 网络中断时文件不损坏（已 flush 的部分保留），重连后新文件续写；
- 可选上行：PC 选一段 WAV 通过 TCP 推给设备播放（会议回放场景）。

---

## 6. 双模式对比与切换

| 维度 | UDP 通话模式 | TCP 录音模式 |
|---|---|---|
| 优化目标 | 实时性、对话自然 | 完整性、可追溯 |
| 传输协议 | UDP（无连接） | TCP（可靠流） |
| 延迟目标 | **≤100ms 端到端** | 不敏感（秒级缓冲即可） |
| 丢包策略 | 丢包填静音/插值，可接受 ≤2% 丢包 | TCP 重传，**零丢帧** |
| 音频方向 | 双向全双工 | 设备→PC 为主（可选 PC→设备回放） |
| 数据落盘 | 不保存（实时流） | 整段 WAV 文件 + 索引 |
| 带宽占用 | 32KB/s（裸 PCM）或 8KB/s（G.722） | 32KB/s 连续 |
| 切换方式 | 设备 UI 触控 / 长按 BOOT 3s 循环切换；PC 端通过 UDP 信令 50003 同步模式 | |

---

## 7. PC 端软件规格

| 组件 | 技术选型 | 职责 |
|---|---|---|
| 主框架 | Python 3.10+ / 跨平台 | 命令行 + 可选 Tkinter/PyQt 简易 GUI |
| UDP 通话 | socket + pyaudio + numpy | 收发包、jitter buffer、扬声器播放、麦克采集 |
| TCP 录音 | socket + wave/标准库 | TCP Server、WAV 落盘、切片、索引、断点续传 |
| 设备发现 | UDP 广播 50003 | 启动时扫描局域网内设备，返回 IP/模式/固件版本 |
| 日志 | logging | 分级日志，记录丢包率、延迟、录音文件路径 |

> **参考开源**：Seeed reSpeaker XIAO ESP32S3 UDP Streaming（Python 服务端存 WAV）、ESP Microphone（pyaudio 收播+录 WAV）、woodif/ESP32S3_I2S_Mic_Stream_Rec（WAV patch header + 2s flush 防掉电）、PCMFlow722（G.722 宽带语音压缩）。

---

## 8. 性能与验收指标

| 指标 | 目标值 |
|---|---|
| 本地音频回环延迟 | ≤8ms |
| UDP 端到端（局域网） | ≤100ms |
| TCP 录音完整性 | 0 丢帧 |
| LVGL 稳态帧率 | 30FPS |
| 任务调度抖动 | ≤1ms |
| 连续运行 | ≥72h 无宕机 |
| 录音自动切片 | 30min |
| 录音 flush 周期 | 2s |

---

## 9. 里程碑

| 阶段 | 内容 | 出口标准 |
|---|---|---|
| M1 音频链路 | ES8389 TDM 初始化、I2S DMA 双缓冲、本地回环 | 回环 ≤8ms，无爆音 |
| M2 UDP 通话 | 设备 UDP 发 + PC pyaudio 收播 + 双向对讲 | 端到端 ≤100ms，丢包补偿听感连续 |
| M3 TCP 录音 | 设备 TCP Client + PC Server WAV 落盘 + 切片 + 断点续传 | 录音文件完整可播，拔线不损坏 |
| M4 UI 与双模式切换 | LVGL 三屏、按键切换、设备发现、信令同步 | 一键切换不重启，状态 UI 同步 |
| M5 稳定与优化 | AEC、G.722 可选、Wi-Fi 自适应、看门狗、72h 老化 | 无宕机、内存无碎片、指标全达标 |
